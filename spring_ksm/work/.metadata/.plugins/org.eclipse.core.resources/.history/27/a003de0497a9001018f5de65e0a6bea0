@charset "UTF-8";

/* 전체 화면 */
body {
  font-family: 'Noto Sans KR', sans-serif;
  background: linear-gradient(135deg, #f0f4ff, #d9e8ff);
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  margin: 0;
}

/* 메인 박스 */
#main_box {
  width: 520px;
  background: #ffffff;
  padding: 50px 40px;
  border-radius: 20px;
  box-shadow: 0 6px 20px rgba(0, 0, 0, 0.12);
  text-align: center;
}

/* 부모 div도 넓게 */
.type_content {
  width: 100%;
  display: block;
}

/* input */
#main_box input[type="text"],
#main_box input[type="email"],
#main_box input[type="number"],
#main_box input[type="password"] {
  display: block;
  width: 100%;           
  max-width: 100%;
  padding: 18px 20px;
  margin-bottom: 20px;
  font-size: 17px;
  border: 2px solid #c5d4f5;
  border-radius: 12px;
  background: #f9fbff;
  outline: none;
  transition: all 0.25s ease-in-out;
  box-sizing: border-box;
}

/* placeholder */
#main_box input::placeholder {
  color: #a0a8b8;
  font-size: 16px;
}

/* 포커스 */
#main_box input:focus {
  border-color: #4a74f8;
  background: #ffffff;
  box-shadow: 0 0 12px rgba(74, 116, 248, 0.35);
}

/* disabled */
#main_box input:disabled {
  background: #e9edf7;
  color: #6b7280;
  cursor: not-allowed;
  opacity: 0.7;
}

/* 버튼 */
#main_box input[type="button"] {
  display: block;
  width: 100%;
  max-width: 100%;
  padding: 18px;
  font-size: 17px;
  font-weight: 600;
  background: linear-gradient(135deg, #4a74f8, #5c8efb);
  color: #ffffff;
  border: none;
  border-radius: 12px;
  cursor: pointer;
  transition: transform 0.2s ease, background 0.2s ease;
  margin-top: 4px;
}

#main_box input[type="button"]:hover {
  background: linear-gradient(135deg, #3a63e6, #507ff5);
  transform: translateY(-2px);
}

#main_box input[type="button"]:active {
  transform: translateY(0);
}

/* 인증 결과 텍스트 */
#mail_check_warn {
  margin-top: 15px;
  font-weight: bold;
  font-size: 16px;
}

#mail_check_warn:empty {
  display: none;
}

#mail_check_warn:contains("성공") {
  color: #28a745;
}

#mail_check_warn:contains("불일치") {
  color: #dc3545;
}
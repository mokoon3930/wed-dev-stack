<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="v">
		
	<select id="visit_list" resultType="visit">
		select * from visit
	</select>
	
	<insert id="insert_visit" parameterType="visit">
    insert into visit (idx, content, name, ip, pwd, regdate)
    values (seq_visit_idx.nextVal, #{content}, #{name}, #{ip}, #{pwd}, sysdate)
	</insert>
	
	<select id="visit_one" parameterType="int" resultType="visit">
		select * from visit where idx=#{idx}
	</select>
	
	<update id="update_visit" parameterType="visit">
		update visit set content=#{content}, name=#{name}, ip=#{ip}, pwd=#{pwd}, regdate = sysdate
		where idx=#{idx}
	</update>
	
	<select id="checkPwd" parameterType="int" resultType="string">
    SELECT pwd 
    FROM visit 
    WHERE idx = #{idx}
	</select>
		
	<delete id="del_visit" parameterType="int">
		delete from visit where idx=#{idx}
	</delete>	
		
		
</mapper>












